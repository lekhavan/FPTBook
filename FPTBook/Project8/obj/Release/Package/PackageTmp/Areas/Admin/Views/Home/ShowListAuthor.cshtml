@model IEnumerable<WebBanSach.Models.Data.TacGia>

@{
    ViewBag.Title = "Quản lý tác giả";
    Layout = "~/Areas/Admin/Views/Shared/_LayoutAdmin.cshtml";
}

<h2>Author Management</h2>

<p>
    @Html.ActionLink("Add New Author", "AddAuthor")
</p>
<table class="table" id="myTable">
    <thead>
        <tr>
            <th>
                Author ID
            </th>
            <th>
                Author Name
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr id="row_@item.MaTG">
                <td>
                    @Html.DisplayFor(modelItem => item.MaTG)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.TenTG)
                </td>
                <td>
                    @Html.ActionLink("View", "UpdateAuthor", new { id = item.MaTG }) |
                    @Ajax.ActionLink("Delete", "DeleteAuthor", new { id = item.MaTG }, new AjaxOptions
               {
                   Confirm = "Are you sure to delete this author?",
                   OnComplete = "$('#row_" + @item.MaTG + "').remove()",
                   HttpMethod = "Delete"
               })
                </td>
            </tr>
        }
    </tbody>

</table>
